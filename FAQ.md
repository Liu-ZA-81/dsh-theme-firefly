# FAQ · 常见问题

## 安装与使用

### Q：安装后主题没生效？

1. 确认已经跑过构建：`node build.cjs`（会生成 `lib/client.js`）
2. 确认已重启 `dsh web`
3. 打开 `设置`，确认流萤主题已启用（若装了插件市场，可在「主题」页切换）

### Q：为什么「动态壁纸」按钮是灰色的？

因为 `assets/` 里没有 `.mp4` 文件。动态壁纸是可选功能：把已授权/原创的
`.mp4` 放进 `assets/` 后重新 `node build.cjs`，按钮即可用。

### Q：如何更换壁纸 / 音乐 / 开屏动画？

| 素材 | 位置 | 说明 |
|---|---|---|
| 壁纸（图片/视频） | `assets/` | 支持 jpg/png/webp/mp4 |
| 开屏动图 | `GIF/` | 取第一个 .gif |
| 表情包 | `GIF/表情包/` | 文件名即触发情绪 |
| 背景音乐 | `music/` | 支持 mp3/ogg/m4a/wav |

换完后重新运行 `node build.cjs`，再刷新页面。

### Q：为什么 `lib/client.js` 那么大（几十 MB）？

DSH 只服务插件的 `client.js` 一个文件，所以图片/视频/音频/GIF 都以 base64
内嵌进去。控制素材体积（视频 ≤1080p、音乐 ≤128kbps）可显著减小。

## 功能与操作

### Q：右下角几个按钮分别是什么？

| 按钮 | 功能 |
|---|---|
| 声 | 打字音效 开/关 |
| 萤 | 氛围粒子分档（关/星点/曳光/流萤） |
| 景 | 壁纸设置（类型 + 切换/随机 + 随机间隔） |
| 乐 | 背景音乐（播放卡片：上下曲 + 循环模式） |

### Q：表情包彩蛋怎么触发？

按对话内容自动触发（每回合最多一个）：

- 你夸赞（谢谢/太棒/厉害）→ 开心 / 得意
- 你确认开干（开始/开干/动手）→ 变身
- 我确认（没错/正是）→ 没错
- 我请你提供（发我/提供/给我）→ 期待
- 我请你确认（确认一下/要我…吗）→ 疑惑

另外单独发送 `SAM` 会重播开屏变身动画。

### Q：如何关闭音效 / 粒子 / 音乐？

- 打字音效：右下角「声」按钮
- 萤火粒子：右下角「萤」→ 选「关」
- 背景音乐：右下角「乐」按钮

### Q：如何彻底卸载本主题？

```powershell
dsh plugin --profile web remove dsh-theme-firefly
```

重启 `dsh web` 生效。

## 其它

### Q：能否发布到 npm？

可以，但本主题体积较大（含内嵌素材），git 安装体验反而更稳定。如需 npm 分发，
建议先精简素材。

### Q：有 B 站演示视频吗？

见 README「预览与演示」章节的视频链接。
