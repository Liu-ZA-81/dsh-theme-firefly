# FAQ · 常见问题

## 安装与使用

### Q：安装后主题没生效？

1. 确认已经跑过构建：`node build.cjs`（会生成 `lib/client.js`）
2. 确认已重启 `dsh web`
3. 打开 `设置`，确认流萤主题已启用（若装了插件市场，可在「主题」页切换）

### Q：为什么「动态」按钮是灰色的？

因为当前没有任何视频壁纸。用两种方式加一个即可：

- 点「景」→「＋ 添加壁纸」导入一个 `.mp4`（推荐，立即生效）
- 或把 `.mp4` 放进 `assets/` 后重新 `node build.cjs`

### Q：如何更换壁纸 / 音乐 / 开屏动画？

**壁纸**：点「景」→「＋ 添加壁纸」直接导入（立即生效）；或放入 `assets/` 后构建。

其余素材通过 `build.cjs` 内嵌：

| 素材 | 位置 | 说明 |
|---|---|---|
| 壁纸（图片/视频） | `assets/` | 支持 jpg/png/webp/mp4 |
| 开屏动图 | `GIF/` | 取第一个 .gif |
| 表情包 | `GIF/表情包/` | 文件名即触发情绪 |
| 背景音乐 | `music/` | 支持 mp3/ogg/m4a/wav |
| 歌曲默认封面 | `music/figure/` | 取第一张图片（如知更鸟图） |
| 音乐排除清单 | `build.music-exclude.txt` | 一行一个文件名，构建时不内嵌 |

换完后重新运行 `node build.cjs`，再刷新页面。

### Q：首次安装的默认壁纸是哪个？

首次安装（本地还没有任何壁纸记录）时会优先展示 `assets/` 里文件名含 `Default`
的图片，仓库默认随带的是 `Default_wallpaper.png`。之后你手动切换过壁纸，就会以
你的选择为准（记录在浏览器本地）。想回到默认图，清一下该站点的本地存储即可。

### Q：为什么 `lib/client.js` 那么大（几十 MB）？

DSH 只服务插件的 `client.js` 一个文件，所以图片/视频/音频/GIF 都以 base64
内嵌进去。控制素材体积（视频 ≤1080p、音乐 ≤128kbps）可显著减小。

## 功能与操作

### Q：几个按钮分别是什么？

| 按钮 | 功能 |
|---|---|
| 声 | 打字音效 开/关 |
| 萤 | 氛围粒子分档（关/星点/曳光/流萤） |
| 景 | 壁纸设置（类型 + 选择/随机 + 随机间隔 + 添加壁纸） |
| 乐 | 背景音乐（面板：唱片/封面/进度/上下曲/循环/选择/添加 + 迷你播放器） |

四个按钮放在一个**可拖动的毛玻璃长条面板**里（见下方「如何移动按钮面板」）。

### Q：如何移动按钮面板？位置会记住吗？

按住面板任意位置拖动即可移动，位置存到浏览器本地，重启 `dsh web` 后仍然有效。
当面板太靠屏幕边缘时，弹出的面板会自动贴边收在屏幕内，避免跑出屏幕。

### Q：如何移除误导入 / 不想要的壁纸？

点「景」→ 模式「选择」→ 在网格里勾选要删的壁纸 → 点「移除」。

- 运行时导入的壁纸：真正从 IndexedDB 删除，刷新后不再出现；
- 内置壁纸：只能「隐藏」（记录在浏览器本地），因为它已打包进 client.js；
  想彻底去掉需从 `assets/` 删除对应文件并重新构建。

### Q：如何移除 / 筛选歌曲？

在音乐面板点「选择」，勾选后：

- 「移除」：内置歌曲记隐藏、导入歌曲真正删除（IndexedDB）；
- 「随机」：把这批勾选歌曲设为随机播放池并进入随机模式。

### Q：如何让「随机」只轮换我选中的几张？

在「选择」网格里勾选若干张 → 点「随机」，即把这批设为随机轮换池并立即进入随机模式。
不勾选则代表「全部参与随机」。

### Q：如何一次导入多张壁纸？

点「景」→「＋ 添加壁纸」，在文件选择窗口里按住 Ctrl 多选、框选或 Shift 连选即可。
同名且同大小的文件会自动去重，不会重复导入。

### Q：表情包彩蛋怎么触发？

按对话内容自动触发（每回合最多一个）：

- 你夸赞（谢谢/太棒/厉害）→ 开心 / 得意
- 你确认开干（开始/开干/动手）→ 变身
- 我确认（没错/正是）→ 没错
- 我请你提供（发我/提供/给我）→ 期待
- 我请你确认（确认一下/要我…吗）→ 疑惑

另外单独发送 `SAM` 会重播开屏变身动画。

### Q：如何关闭音效 / 粒子 / 音乐？

- 打字音效：「声」按钮
- 萤火粒子：「萤」→ 选「关」
- 背景音乐：「乐」按钮
- 浮层（壁纸/氛围/歌单等）：按 **ESC** 一键关闭；音乐面板展开时按 ESC 收起为迷你播放器

### Q：如何彻底卸载本主题？

```powershell
dsh plugin --profile web remove dsh-theme-firefly
```

重启 `dsh web` 生效。

## 其它

### Q：能否发布到 npm？

可以，但本主题体积较大（含内嵌素材），git 安装体验反而更稳定。如需 npm 分发，
建议先精简素材。

### Q：有 B 站演示视频吗？

见 README「预览与演示」章节的视频链接。
